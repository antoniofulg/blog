---
status: pending
title: Admin Route Server Function Extraction
type: refactor
complexity: medium
dependencies:
  - task_02
  - task_03
---

# Task 04: Admin Route Server Function Extraction

## Overview

Extract all `createServerFn` calls and raw database access functions out of `admin/index.tsx` and `admin/preview.$slug.tsx` into co-located `*.server.ts` files. The route files are stripped to route config, `beforeLoad`, loader wiring, and React components only. Both new server files import `requireSession` from `#/lib/session` (task_02) and `renderMdx` from `#/lib/mdx/renderer.server` (task_03). This eliminates the last two inline `requireSession` copies in the codebase.

<critical>
- ALWAYS READ the PRD and TechSpec before starting
- REFERENCE TECHSPEC for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — every task MUST include tests in deliverables
</critical>

<requirements>
- MUST create `app/routes/admin/index.server.ts` containing `getAllPostsFn`, `togglePublishedFn`, `getAllPosts`, `togglePublished` moved verbatim from `admin/index.tsx`
- MUST create `app/routes/admin/preview.$slug.server.ts` containing `getAdminPreviewFn`, `getAdminPreview` moved verbatim from `admin/preview.$slug.tsx`
- MUST import `requireSession` from `#/lib/session` in both new server files (not inline)
- MUST update `admin/index.tsx` to import server fns from `./index.server`; file MUST contain only route config, `beforeLoad`, loader wiring, and React components after cleanup
- MUST update `admin/preview.$slug.tsx` to import server fn from `./preview.$slug.server`; file MUST contain only route config, `beforeLoad`, loader wiring, and React components after cleanup
- MUST remove all inline `requireSession` function definitions from both route files
- MUST import `renderMdx` from `#/lib/mdx/renderer.server` in `preview.$slug.server.ts` (not from old `#/lib/mdx.server`)
- MUST pass `tsc --noEmit` with zero errors after all changes
- MUST NOT move `beforeLoad` or React components to server files — those stay in `*.tsx`
</requirements>

## Subtasks

- [ ] 4.1 Create `app/routes/admin/index.server.ts` — move all server fns and DB access functions; import `requireSession` from `#/lib/session`
- [ ] 4.2 Create `app/routes/admin/preview.$slug.server.ts` — move server fn; import `requireSession` from `#/lib/session` and `renderMdx` from `#/lib/mdx/renderer.server`
- [ ] 4.3 Strip `admin/index.tsx` — remove moved exports, add import from `./index.server`, remove inline `requireSession`
- [ ] 4.4 Strip `admin/preview.$slug.tsx` — remove moved exports, add import from `./preview.$slug.server`, remove inline session check
- [ ] 4.5 Run `tsc --noEmit` and verify zero `requireSession` definitions remain in route files (`grep -r "async function requireSession" app/routes/`)

## Implementation Details

See TechSpec "Core Interfaces" for `index.server.ts` and `preview.$slug.server.ts` export signatures, and "File-by-File Changes" for the complete list of what moves and what stays.

Current state of files to strip:
- `admin/index.tsx` (184 lines) — server fns at top; `requireSession` inline at lines 36-39; React component at bottom
- `admin/preview.$slug.tsx` (78 lines) — server fn at top; inline session check at lines 32-35; React component at bottom

After extraction, each route file should be significantly shorter (primarily `createFileRoute`, `beforeLoad`, `loader`, and component function).

### Relevant Files

- `app/routes/admin/index.tsx` — source file; server fns and inline `requireSession` at lines 36-39 must be removed
- `app/routes/admin/preview.$slug.tsx` — source file; server fn and inline session check at lines 32-35 must be removed
- `app/lib/session.ts` — created in task_02; `requireSession` imported from here in both new server files
- `app/lib/mdx/renderer.server.ts` — created in task_03; `renderMdx` imported from here in `preview.$slug.server.ts`

### Dependent Files

- `app/tests/admin-routes.test.ts` — tests `getAllPostsFn`, `togglePublishedFn`, `getAdminPreviewFn`; import paths must update if test imports from route files directly
- `app/routeTree.gen.ts` — auto-generated by TanStack Router; regenerate with `bunx tsr generate` or by starting dev server after route file changes

### Related ADRs

- [ADR-001: Scope bounded to server fn extraction + shared auth util + docs](adrs/adr-001.md) — defines the two-file pattern as the V1 target
- [ADR-004: `requireSession` uses throw-only pattern (`Promise<void>`)](adrs/adr-004.md) — server files call `await requireSession()` as first statement in handler; return type is void

## Deliverables

- `app/routes/admin/index.server.ts` with all four exports (`getAllPostsFn`, `togglePublishedFn`, `getAllPosts`, `togglePublished`)
- `app/routes/admin/preview.$slug.server.ts` with `getAdminPreviewFn` and `getAdminPreview`
- `admin/index.tsx` stripped to route config + UI only; imports server fns from `./index.server`
- `admin/preview.$slug.tsx` stripped to route config + UI only; imports server fn from `./preview.$slug.server`
- Zero remaining inline `requireSession` definitions in `app/routes/`
- `tsc --noEmit` exits 0
- Existing admin route test suite passes (REQUIRED)

## Tests

- Unit tests:
  - [ ] `tsc --noEmit` passes — all imports from new server files resolve correctly
  - [ ] `app/tests/admin-routes.test.ts` passes — `getAllPostsFn`, `togglePublishedFn`, and `getAdminPreviewFn` must behave identically after move (verbatim body move)
  - [ ] `grep -r "async function requireSession" app/routes/` returns zero matches — confirms all inline copies removed
- Integration tests:
  - [ ] `app/tests/auth-integ.test.ts` passes — admin route session protection must work end-to-end with extracted `requireSession`
  - [ ] `app/tests/sync-integ.test.ts` passes — no regressions in routes that depend on admin server fns
- Test coverage target: existing suite coverage maintained; no new code paths (verbatim moves only)
- All tests must pass

## Success Criteria

- All tests passing (`make test`)
- `tsc --noEmit` exits 0
- `app/routes/admin/index.server.ts` and `app/routes/admin/preview.$slug.server.ts` exist
- Both route `*.tsx` files contain no `createServerFn` calls and no `requireSession` definitions
- Both server files import `requireSession` from `#/lib/session`
- `preview.$slug.server.ts` imports `renderMdx` from `#/lib/mdx/renderer.server`
- `make lint` passes — import direction rules not violated
