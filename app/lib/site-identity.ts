/**
 * Site-level identity constants used across the public surface.
 *
 * Centralised so a fork / rename touches one file rather than every
 * component that embeds the author's identity. Adds a single source of
 * truth for screen-reader fallbacks, default alt text, and any future
 * site-author references.
 */
export const SITE_AUTHOR = "Antonio Fulgencio";
