Invoke the `app-audit` skill from `.agents/skills/app-audit/SKILL.md`.

Use it to:
- Run a browser sweep across all routes × locales × auth-states for runtime issues
- Check console errors, hydration mismatches, network failures, missing meta tags, broken images, mixed-content warnings, slow responses, a11y violations, and Lighthouse scores
- Run the app-audit CLI (`bun run audit:fe`) and interpret results
- Check `docs/audits/SUMMARY.md` for audit history and trend

Read `.agents/skills/app-audit/SKILL.md` for full instructions before acting.
